fn main() {
    let buf, p, q;
    buf = malloc(2 * 4);     // buf : *int
    p   = &buf;              //  p : **int
    (*p)[1] = 17;            // deref once, index into the array
    q = &(*p)[1];            // q : *int  → address of that element
    print("{}", *q);         // → 17
}